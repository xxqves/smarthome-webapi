# Smart Home WebAPI
Smart Home WebAPI - это проект с микросервисной архитектурой для управления устройствами умного дома. Он позволяет регистрировать устройства и управлять ими через веб-приложение или API.

# 🚀 Технологии

* Язык: C#

* Фреймворк: ASP.NET Core

* ORM: Entity Framework Core

* База данных: PostgreSQL

* Сообщения/Очереди: RabbitMQ + MassTransit

* Валидация: FluentValidation

* Тестирование: xUnit, FluentAssertions

* Контейнеризация: Docker

# 📦 Установка и запуск
1. Клонируйте репозиторий:
```bash
git clone https://github.com/xxqves/smarthome-webapi.git
cd smarthome-webapi
```
2. В корне проекта перейдите в папку `SmHm.WebApi` и создайте файл `appsettings.json`.  
Скопируйте код ниже и замените значения на свои:

- **ConnectionStrings → SmartHomeDbContext**: строка подключения к PostgreSQL  
- **JwtOptions → SecretKey**: секретный ключ для генерации JWT токенов  
- **RabbitMqOptions**: настройки подключения к серверу RabbitMQ  

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "SmartHomeDbContext": "Host=localhost;Database=smhm_database;Username=postgres;Password=your_password"
  },
  "JwtOptions": {
    "SecretKey": "your_secret_key_here",
    "Audience": "your_audience",
    "Issuer": "your_issuer",
    "ExpiresHours": "12"
  },
  "RabbitMqOptions": {
    "HostName": "localhost",
    "UserName": "your_rabbitmq_user",
    "Password": "your_rabbitmq_password"
  },
  "AllowedHosts": "*"
}
```
3. Запустите систему через Docker
```bash
docker-compose up --build
```
4. Примените миграции Entity Framework Core
```bash
dotnet ef database update
```
5. Запустите Dotnet приложение
```bash
dotnet run
```

# 🧪 Тестирование
Запуск юнит и интеграционных тестов
```bash
dotnet test
```

# 📄 API
В проекте реализованы эндпоинты для:
* CRUD-операций с девайсами, а также просмотр статуса и взаимодействие
* CRUD-операций с комнатами
* Регистрации и авторизации пользователя

Swagger UI доступен по адресу: `http://localhost:your_port/swagger`
your_port - здесь необходимо ввести порт, на котором запустилось веб-приложение, но обычно этого не требуется делать, ведь при запуске системы - автоматически откроется вкладка в браузере с API документацией

# 📂 Структура проекта
```text
SmHm/
├── SmHm.Application/            # Логика приложения, CQRS, сервисы
├── SmHm.Contracts/              # Общие DTO для всех микросервисов
├── SmHm.Core/                   # Основные доменные сущности и бизнес‑логика
├── SmHm.Infrastructure/         # Инфраструктурные реализации (JWT аутентификация, реализация межсервисного общения с помощью RabbitMQ и MassTransit)
├── SmHm.IntegrationTests/       # Интеграционные тесты
├── SmHm.NotificationService/    # Сервис уведомлений (queue/listener)
├── SmHm.Persistence.PostgreSql/ # Реализация доступа к PostgreSQL, DbContext, конфигурации для сущностей
├── SmHm.UnitTests/              # Юнит‑тесты
├── SmHm.WebApi/                 # Основной ASP.NET Core WebAPI проект
├── docker-compose.yml           # Конфигурация docker compose
├── Dockerfile                   # Dockerfile для WebAPI
├── SmHm.sln                     # .NET решение
├── README.md                    # README текущий
├── .gitignore                   # Игнор‑файл для Git
└── LICENSE.txt                  # Лицензия MIT
```

# ⚡ Особенности
* Асинхронное взаимодействие с устройствами через RabbitMQ
* Полная валидация входящих данных через FluentValidation
* Покрытие тестами с FluentAssertions и xUnit
* Контейнеризация для быстрого развёртывания
